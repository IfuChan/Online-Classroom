<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.classroom">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Classroom"
        android:usesCleartextTraffic="true">
        <activity android:name=".LectureView"> </activity>
        <activity android:name=".view" />
        <activity android:name=".pdfViewer"/>
        <activity android:name=".LogInTHpage" />
        <activity android:name=".TProfile" />
        <activity android:name=".sProfile" />
        <activity android:name=".LoggedInHpage" />
        <activity android:name=".SignIn" />
        <activity android:name=".Reg" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>